\section{源基准及其适配依据}

本文选择 $\tau$-bench、BFCL、API-Bank 和 AppWorld，它们分别覆盖用户交互、通用函数调用、工具检索与跨应用执行等典型任务形态，且都提供了可追溯的调用证据。但这些证据在原论文中的含义并不相同：有的保存目标动作，有的保存多轮参考调用，有的记录单次 API 请求—结果，有的由最终状态评测器判定任务。本节先回到各基准的任务设计和数据结构。

\subsection{\texorpdfstring{$\tau$-bench}{tau-bench}}

\subsubsection{任务结构与评测机制}

$\tau$-bench 面向航空和零售客服场景\citep{yao2025taubench}。每个任务由用户指令和一组参考动作（actions）组成：用户指令是一段自然语言，描述用户想完成什么（如取消预订、交换商品、修改地址）；原论文中的 actions 主要包含达到目标状态所需的数据库写操作和需要向用户传达的输出信息，读取操作允许智能体自行决定。零售域有 115 条任务，航空域有 50 条，共 165 条。

任务环境包含结构化数据库：用户档案（email、地址、支付方式、出生日期）、订单、商品、航班和预订记录。任务指令对智能体不可见，智能体通过与用户模拟器的多轮对话逐步获取任务信息并调用工具；用户模拟器同样看不到工具执行过程，只能看到智能体的自然语言回复。智能体必须遵守退款、改签、身份核验等业务规则。评测不逐字匹配固定轨迹，而是比较执行后的数据库状态是否与标注一致，并结合任务要求检查最终回复是否包含用户所需信息。论文采用 pass$^k$ 衡量重复试验中的稳定成功率。

\begin{figure}[t]
  \centering
  \begin{tikzpicture}[
    font=\small,
    box/.style={rounded corners=2pt, draw, thick, align=center, inner sep=3pt, minimum height=6mm},
    userbox/.style={box, draw=RPLBlue, fill=RPLBlue!5, text width=2.4cm},
    agentbox/.style={box, draw=RPLGreen, fill=RPLGreen!5, text width=2cm},
    dbbox/.style={box, draw=gray, fill=gray!8, text width=2.4cm},
    evalbox/.style={box, draw=RPLOrange, fill=RPLOrange!8, text width=3cm},
    filebox/.style={box, draw=RPLDark, fill=RPLDark!5, text width=2.4cm, font=\footnotesize},
    adaptbox/.style={box, draw=RPLRed, fill=RPLRed!4, text width=3.5cm, font=\footnotesize},
    arrow/.style={-{Latex[length=2mm]}, thick},
    biarrow/.style={{Latex[length=2mm]}-{Latex[length=2mm]}, thick},
    darrow/.style={-{Latex[length=2mm]}, thick, dashed},
  ]
    % --- 上层：原 benchmark 结构 ---
    \node[userbox] (user) {用户模拟器};
    \node[agentbox, below=6mm of user] (agent) {Agent};
    \node[box, draw=RPLBlue, fill=RPLBlue!3, text width=1.4cm, left=6mm of agent] (policy) {领域};
    \node[dbbox, below=6mm of agent] (db) {数据库工具};
    \node[evalbox, below=5mm of db] (eval) {评测};
    \node[font=\footnotesize, below=0mm of eval, text width=3cm, align=center] {DB 状态 + 输出检查};

    \draw[biarrow] (user) -- node[right, font=\footnotesize] {对话} (agent);
    \draw[biarrow] (agent) -- node[right, font=\footnotesize] {读写} (db);
    \draw[arrow] (policy) -- (agent);
    \draw[arrow] (db) -- (eval);

    % --- 分隔 ---
    \node[draw=none, below=3mm of eval] (sep) {\footnotesize\itshape 源码文件};

    % --- 下层：源码 → Adapter → 输出 ---
    \node[filebox, below=2mm of sep, xshift=-2.8cm] (f1) {\texttt{tasks\_test.py}\\\footnotesize user\_id, instruction, actions};
    \node[filebox, below=2mm of f1] (f2) {\texttt{users.json}\\\footnotesize 用户档案};
    \node[filebox, below=2mm of f2] (f3) {\texttt{tools/*.py}\\\footnotesize get\_info()};

    \node[adaptbox, right=15mm of f2] (adapter) {TauBenchAdapter};
    \node[filebox, draw=RPLGreen, fill=RPLGreen!5, right=10mm of adapter, text width=2.8cm] (out) {instruction\\source\_state\\tools, calls};

    \draw[arrow] (f1) -- (adapter);
    \draw[arrow] (f2) -- (adapter);
    \draw[arrow] (f3) -- (adapter);
    \draw[arrow] (adapter) -- (out);
  \end{tikzpicture}
  \caption{$\tau$-bench 的原 benchmark 结构（上）与源码文件到适配器输出的对应关系（下）。Agent 与数据库工具之间是双向读写关系。}
  \label{fig:tau-structure}
\end{figure}

\subsubsection{适配依据}

本文采用的 \code{tasks\_test.py} 提供了完整的 typed reference sequence：其中不仅包含写操作，还包含查询（如 \code{get\_user\_details}、\code{get\_reservation\_details}）、搜索（如 \code{search\_direct\_flight}）和计算（如 \code{calculate}）等中间步骤。本文将这些读、写和计算步骤统一作为源参考动作，通过源重放和环境状态检查确认可用性。

$\tau$-bench 的用户、订单和支付方式都保存在结构化数据库中，用户身份可以由任务标识和数据库记录交叉核对，适合提取用户档案。部分源参考序列在原生执行中会暴露业务错误（如查询不存在的商品、对非已交付订单执行换货），本文对这些序列采取保守过滤。

\subsection{BFCL}

\subsubsection{任务结构与评测机制}

BFCL 是一个综合性函数调用基准，覆盖单轮、并行、多语言和多轮场景\citep{patil2025bfcl}。其多轮部分围绕八组状态化 API（交易、旅行、文件系统、消息、工单等）构造任务，每条任务包含一个多轮查询序列和对应的参考调用轨迹。一个用户轮次（turn）中可能包含多个工具调用步骤（step），参考轨迹按轮次组织但保留轮内多调用结构。多轮数据有四个变体：base 是标准多轮任务；miss\_param 故意省略参数以测试追问能力；miss\_func 移除函数以测试澄清能力；long\_context 加长上下文以测试长程准确性。

每条任务的数据结构包括：\code{question}（多轮用户查询）、\code{possible\_answer}（逐轮参考调用）、\code{initial\_config}（初始状态，如预认证会话、账户余额）和 \code{involved\_classes}（涉及的 API 类）。评测同时使用 state-based checker 检查最终系统状态是否匹配，以及 response-based checker 检查产生请求结果所必需的调用路径——后者尤其用于无法通过状态变化判断的只读任务。
\begin{figure}[t]
  \centering
  \begin{tikzpicture}[
    font=\small,
    box/.style={rounded corners=2pt, draw, thick, align=center, inner sep=3pt, minimum height=6mm},
    filebox/.style={box, draw=RPLDark, fill=RPLDark!5, text width=3cm, font=\footnotesize},
    databox/.style={box, draw=RPLBlue, fill=RPLBlue!5, text width=2.4cm, font=\footnotesize},
    evalbox/.style={box, draw=RPLOrange, fill=RPLOrange!8, text width=3cm},
    adaptbox/.style={box, draw=RPLRed, fill=RPLRed!4, text width=2.4cm, font=\footnotesize},
    outbox/.style={box, draw=RPLGreen, fill=RPLGreen!5, text width=2.6cm, font=\footnotesize},
    arrow/.style={-{Latex[length=2mm]}, thick},
    darrow/.style={-{Latex[length=2mm]}, thick, dashed},
  ]
    % --- 上层：原 benchmark 数据结构 ---
    \node[filebox] (q) {\texttt{BFCL\_v4\_multi\_turn\_base.json}\\\footnotesize question, initial\_config, involved\_classes};
    \node[filebox, below=3mm of q] (pa) {\texttt{possible\_answer/...base.json}\\\footnotesize ground\_truth[turn][call]};
    \node[filebox, below=3mm of pa] (fd) {\texttt{multi\_turn\_func\_doc/*.json}\\\footnotesize function schemas};

    % --- 分隔 ---
    \node[draw=none, below=3mm of fd] (sep) {\footnotesize\itshape 适配};

    % --- 下层：Adapter → 输出 ---
    \node[adaptbox, below=2mm of sep] (adapter) {BfclAdapter};
    \node[outbox, right=10mm of adapter] (out) {instruction\\source\_state\\visible tools\\calls + turn\_boundaries};

    % --- 评测 ---
    \node[evalbox, below=8mm of adapter] (eval) {BFCL state checker};
    \node[font=\footnotesize, below=0mm of eval, text width=3cm, align=center] {state + response};

    \draw[arrow] (q) -- (pa);
    \draw[arrow] (pa) -- (fd);
    \draw[arrow] (fd) -- (adapter);
    \draw[arrow] (adapter) -- (out);
    \draw[arrow] (out.south) |- (eval.east);
  \end{tikzpicture}
  \caption{BFCL 多轮数据的文件结构与适配器映射。\texttt{initial\_config} 是整个任务的初始状态，\texttt{possible\_answer} 提供逐轮参考调用，\texttt{multi\_turn\_func\_doc} 提供函数 schema。}
  \label{fig:bfcl-structure}
\end{figure}

\subsubsection{适配依据}

本文使用 200 条 base 多轮任务。\code{possible\_answer} 保存逐轮参考调用，\code{initial\_config} 提供可恢复的初始状态。对于会修改状态的任务，其他调用顺序可能同样正确；对于查询型任务，响应检查会对必要步骤提出更强约束。

BFCL 的主要适配挑战是语义对齐：相同字符串可能在不同 API 类中表示密码、访问令牌、股票代码或普通文本，用户指令中的身份也可能与某个默认 profile 冲突。本文只接受能够由 API 类、参数名、初始状态和指令身份共同支持的字段映射；不能确定时放弃该字段或过滤任务。

\subsection{API-Bank}

\subsubsection{任务结构与评测机制}

API-Bank 将工具调用能力分为三个递进层级\citep{li2023apibank}。Level-1（直接调用）给定 API 定义，评测模型能否按查询正确填参调用，本质是参数填充。Level-2（检索后调用）API 未知且数量大，模型须先用 ToolSearcher 按关键词检索相关 API，再为每个分解后的子查询检索并调用一个 API。Level-3（规划—检索—调用）要求模型自主规划并多步调用，难度最高。评测集分别有 214、50 和 50 条对话。

其 73 个 API 由工程师真实实现，涵盖天气、账号管理、日程、健康和财务等领域。对话以多轮形式组织，每个 AI 回合可能产出 API 请求，系统执行后回填结果。每条 API 记录保存请求参数（param\_dict）和返回结果（result），ToolSearcher 是一个特殊 API，输入关键词后返回最相关 API 的元信息。

评测方式为：初始化 API 数据库后，比较预测调用与人工标注调用是否产生相同查询或修改，并检查返回结果是否一致。API 调用使用 Accuracy 评价，调用后的自然语言回复使用 ROUGE-L 评价。这是一种逐调用的一致性检查，而非 AppWorld 那种整段任务的最终状态 evaluator。

\begin{figure}[t]
  \centering
  \begin{tikzpicture}[
    font=\small,
    box/.style={rounded corners=2pt, draw, thick, align=center, inner sep=3pt, minimum height=6mm},
    l1box/.style={box, draw=RPLGreen, fill=RPLGreen!5, text width=3.2cm},
    l2box/.style={box, draw=RPLBlue, fill=RPLBlue!5, text width=3.2cm},
    l3box/.style={box, draw=gray, fill=gray!4, text width=3.2cm, text=gray},
    filebox/.style={box, draw=RPLDark, fill=RPLDark!5, text width=2.8cm, font=\footnotesize},
    adaptbox/.style={box, draw=RPLRed, fill=RPLRed!4, text width=2.4cm, font=\footnotesize},
    outbox/.style={box, draw=RPLGreen, fill=RPLGreen!5, text width=2.6cm, font=\footnotesize},
    evalbox/.style={box, draw=RPLOrange, fill=RPLOrange!8, text width=3.2cm},
    arrow/.style={-{Latex[length=2mm]}, thick},
    darrow/.style={-{Latex[length=2mm]}, thick, dashed},
  ]
    % --- 上层：三级并列 ---
    \node[l1box] (l1) {Level 1: 直接调用\\\footnotesize API 已知 $\to$ 填参 $\to$ 执行};
    \node[l2box, right=6mm of l1] (l2) {Level 2: 检索后调用\\\footnotesize ToolSearcher $\to$ 检索 $\to$ 调用};
    \node[l3box, right=6mm of l2] (l3) {Level 3: 规划—检索—调用\\\footnotesize \itshape 本文未使用};

    % --- 分隔 ---
    \node[draw=none, below=4mm of l2] (sep) {\footnotesize\itshape 源码文件（Level-1 \& Level-2）};

    % --- 下层：源码 → Adapter → 输出 ---
    \node[filebox, below=2mm of sep, xshift=-1.6cm] (f1) {\texttt{dialogue.jsonl}\\\footnotesize api\_name, param\_dict, result};
    \node[filebox, below=2mm of f1] (f2) {\texttt{apis/*.py}\\\footnotesize 原生 API schema};

    \node[adaptbox, right=10mm of f1, yshift=-3mm] (adapter) {ApiBankAdapter};
    \node[outbox, right=10mm of adapter] (out) {instruction\\calls\\recorded results\\visible tools};

    % --- 评测 ---
    \node[evalbox, below=6mm of adapter] (eval) {评测};
    \node[font=\footnotesize, below=0mm of eval, text width=3.5cm, align=center] {Accuracy + ROUGE-L\\\footnotesize 逐调用检查，非整任务 oracle};

    \draw[arrow] (f1) -- (adapter);
    \draw[arrow] (f2) -- (adapter);
    \draw[arrow] (adapter) -- (out);
    \draw[arrow] (adapter) -- (eval);
  \end{tikzpicture}
  \caption{API-Bank 三级任务结构（上）与源码文件到适配器输出的对应关系（下）。三级是并列的不同难度，非串行流水线。Level-2 中后续工具必须先被 ToolSearcher 暴露。本文只使用 Level-1 和 Level-2。}
  \label{fig:apibank-structure}
\end{figure}

\subsubsection{适配依据}

本文使用 214 条 Level-1 对话和 50 条 Level-2 对话，共 264 条源记录。Adapter 只读取显式 API 行中的结构化 \code{param\_dict} 和 recorded result，并验证 Level-2 中工具是否已被先前的 ToolSearcher 暴露。Level-3 样例没有与上述两层相同的结构化执行契约，因此没有与其混合。



\subsection{AppWorld}

\subsubsection{任务结构与评测机制}

AppWorld 构建了 9 个日常应用（Gmail、Venmo、Amazon、Spotify、SimpleNote、Splitwise、Todoist、Phone、FileSystem）的沙盒环境，包含 457 个 API、101 张数据库表和约 37 万条数据库记录\citep{trivedi2024appworld}。底层数据由 106 名虚构人物和程序化生成的活动数据填充，模拟虚构人物数字生活的合成数据库。任务让智能体完成跨应用的日常数字任务，如“根据 SimpleNote 里的健身计划播放一个时长够今天锻炼的 Spotify 歌单”。

智能体通过“编写代码—执行 API—观察结果—继续执行”的循环完成任务。除 9 个业务应用外，还有两个辅助应用：ApiDocs 提供 API 文档查询，Supervisor 提供任务分派者的地址、支付卡和账号密码等信息。智能体需要先查阅文档了解 API 用法，再编写代码调用 API 并根据返回结果决定下一步操作。

每条任务包含：任务指令、任务专属数据库副本、期望终态值和评测程序。评测采用基于最终数据库状态的程序化单元测试：计算执行前后的数据库 diff，断言期望的变更全部出现且没有附带损害（如任务没让退货却发起退货）。约 15\% 的任务是问答型，还需核对答案正确性。任务分为 train（105）、dev（60）、test\_normal（168）和 test\_challenge（417）四个 split。

\begin{figure}[t]
  \centering
  \begin{tikzpicture}[
    font=\small,
    box/.style={rounded corners=2pt, draw, thick, align=center, inner sep=3pt, minimum height=6mm},
    agentbox/.style={box, draw=RPLGreen, fill=RPLGreen!5, text width=2cm},
    appbox/.style={box, draw=RPLBlue, fill=RPLBlue!5, text width=1.1cm, font=\footnotesize},
    helpbox/.style={box, draw=RPLBlue, fill=RPLBlue!10, text width=1.3cm, font=\footnotesize},
    filebox/.style={box, draw=RPLDark, fill=RPLDark!5, text width=2.8cm, font=\footnotesize},
    adaptbox/.style={box, draw=RPLRed, fill=RPLRed!4, text width=2.6cm, font=\footnotesize},
    outbox/.style={box, draw=RPLGreen, fill=RPLGreen!5, text width=2.6cm, font=\footnotesize},
    evalbox/.style={box, draw=RPLOrange, fill=RPLOrange!8, text width=3cm},
    arrow/.style={-{Latex[length=2mm]}, thick},
    biarrow/.style={{Latex[length=2mm]}-{Latex[length=2mm]}, thick},
    darrow/.style={-{Latex[length=2mm]}, thick, dashed},
  ]
    % --- 上层：原 benchmark 结构 ---
    \node[helpbox] (docs) {ApiDocs};
    \node[helpbox, right=3mm of docs] (sup) {Supervisor};
    \node[agentbox, below=5mm of docs, xshift=3mm] (agent) {Agent};
    \node[appbox, below=5mm of agent, xshift=-2.2cm] (gmail) {Gmail};
    \node[appbox, right=1mm of gmail] (venmo) {Venmo};
    \node[appbox, right=1mm of venmo] (amazon) {Amazon};
    \node[appbox, right=1mm of amazon] (spotify) {Spotify};
    \node[appbox, right=1mm of spotify] (note) {SimpleNote};
    \node[appbox, below=1.5mm of gmail] (split) {Splitwise};
    \node[appbox, right=1mm of split] (todo) {Todoist};
    \node[appbox, right=1mm of todo] (phone) {Phone};
    \node[appbox, right=1mm of phone] (fs) {FileSystem};
    \node[font=\footnotesize\itshape, right=5mm of agent, text width=2cm, align=center]
      {写代码\\$\to$ 执行\\$\to$ 观察};

    \draw[darrow] (docs) -- (agent);
    \draw[darrow] (sup) -- (agent);
    \draw[biarrow] (agent) -- (gmail);
    \draw[biarrow] (agent) -- (spotify);

    % --- 分隔 ---
    \node[draw=none, below=3mm of todo, xshift=0.55cm] (sep) {\footnotesize\itshape 源码文件};

    % --- 下层：源码 → Adapter → 输出 ---
    \node[filebox, below=2mm of sep, xshift=-1.8cm] (f1) {\texttt{api\_calls.json}\\\footnotesize method, url, data};
    \node[filebox, below=2mm of f1] (f2) {\texttt{specs.json, required\_apis}\\\footnotesize 指令, 可见 API};
    \node[filebox, below=2mm of f2] (f3) {\texttt{api\_docs/standard/*.json}\\\footnotesize API schema};

    \node[adaptbox, right=10mm of f2] (adapter) {AppWorldAdapter};
    \node[outbox, right=10mm of adapter] (out) {instruction\\source\_state\\visible tools\\normalized calls};

    % --- 评测（分开） ---
    \node[evalbox, below=6mm of adapter] (eval) {环境评测（独立阶段）};
    \node[font=\footnotesize, below=0mm of eval, text width=3.2cm, align=center] {DB diff 程序化测试};

    \draw[arrow] (f1) -- (adapter);
    \draw[arrow] (f2) -- (adapter);
    \draw[arrow] (f3) -- (adapter);
    \draw[arrow] (adapter) -- (out);
    \draw[darrow] (out.south) |- (eval.east);
  \end{tikzpicture}
  \caption{AppWorld 的原 benchmark 结构（上）与源码文件到适配器输出的对应关系（下）。Adapter 只解析方案 API 日志，环境评测在独立阶段执行。}
  \label{fig:appworld-structure}
\end{figure}

\subsubsection{适配依据}

本文使用 147 条同时具备任务、方案 API 日志（\code{ground\_truth/api\_calls.json}）和可执行验证条件的记录，全部来自 dev 和 train split。test\_normal 和 test\_challenge 的任务没有 \code{api\_calls.json}，只有 \code{answer.json} 和 \code{evaluation.py}，不提供标准调用序列。上游 validation solution 的作用是证明任务可解并为数据生成提供参考执行，

AppWorld 的状态评测能够判断任务目标和数据库副作用，却不直接判断某个已传递参数是否符合当前用途。本文因此另行构造参数级配对调用；其中敏感 optional 参数属于本文的合成扩展，而非 AppWorld 原生接口。

\subsection{证据分层与统一表示}

表\ref{tab:source-characteristics}从任务结构、可见信息、状态环境、参考证据和原始评测五个维度比较四个基准。四个来源最终都会转化为统一调用对象，但统一表示不改变原始证据角色。

\begin{table}[t]
  \centering
  \caption{四个源基准的任务结构与评测机制比较。}
  \label{tab:source-characteristics}
  \scriptsize
  \begin{tabularx}{\textwidth}{L{1.4cm}L{1.6cm}L{1.8cm}L{1.6cm}L{1.8cm}Y}
    \toprule
    来源 & 任务单元 & 智能体可见信息 & 状态环境 & 参考证据 & 原始评测 \\
    \midrule
    $\tau$-bench & 用户模拟任务 & 工具、领域政策、对话 & 航空/零售数据库 & 动作与输出标注 & 数据库状态 + 传达信息 \\
    BFCL & 多轮用户查询 & 函数文档、历史轮次 & 八类状态化 API & \code{possible\_answer} & state + response checker \\
    API-Bank & API 对话 & API 描述或 ToolSearcher & API 数据库与结果 & 人工 API records & 调用一致性 + ROUGE-L \\
    AppWorld & 跨应用数字任务 & ApiDocs、Supervisor、执行历史 & 可重置应用数据库 & validation solution log & DB diff 程序化测试 \\
    \bottomrule
  \end{tabularx}
\end{table}

这种区分直接决定了本文的验证口径。来源重放回答“输出调用是否仍与锁定来源一致”；环境执行回答“调用能否在相应实现中运行”；最终状态验证回答“运行结果是否满足来源任务的状态判据”。三种证据可以同时存在，也可以只具备前两种，
